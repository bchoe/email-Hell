var email = require('./emailLog.json');

/*console.log(email);*/

function lengthEmail(){
  var findEmail = email.emails;
  var orgMail = [];
  for (var i = 0; i < findEmail.length; i ++ ){
    //console.log (findEmail[i]);
    if(findEmail.indexOf(email, fromIndex = 0));
      orgMail.push();
    }
}
lengthEmail();

module.exports = email;